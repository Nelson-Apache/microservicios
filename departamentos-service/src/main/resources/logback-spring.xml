<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- Appender para consola con formato JSON -->
    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- Campos personalizados -->
            <customFields>{"service":"departamentos-service"}</customFields>
            
            <!-- Incluir nombres de logger -->
            <includeContext>false</includeContext>
            
            <!-- Incluir detalles del caller (clase y método) -->
            <includeCallerData>true</includeCallerData>
            
            <!-- Campos a incluir -->
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <version>[ignore]</version>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
        </encoder>
    </appender>

    <!-- Logger raíz -->
    <root level="INFO">
        <appender-ref ref="CONSOLE_JSON" />
    </root>

    <!-- Loggers específicos -->
    <logger name="com.empresa.departamentos" level="INFO" />
    <logger name="org.springframework.web" level="INFO" />
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />

</configuration>
